---
import { cn } from "@/lib/utils";
import { Icon } from "astro-icon/components";

interface Social {
  text: string;
  link: string;
  icon: string;
}

interface Props {
  socials: Social[];
  class?: string;
}

const { socials, class: className } = Astro.props;
---

<ul class="flex space-x-5">
  {
    socials.map((item) => (
      <li class="group tooltip" data-tip={`Go to ${item.text}`}>
        <a href={item.link} title={item.text} aria-label={item.text} class="inline-flex items-center justify-center min-w-[48px] min-h-[48px]">
          <Icon
            name={item.icon}
            class={cn("text-primary group-hover:opacity-50 duration-300 ease-in-out", className)}
          />
        </a>
      </li>
    ))
  }
</ul>
